<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <item
        android:id="@+id/system_keyboard"
        android:icon="@mipmap/system_keyboard"
        android:title="@string/sys_key"
        app:showAsAction="never" />
    <item
        android:id="@+id/input_mode"
        android:icon="@mipmap/input_mode"
        android:title="@string/operator_mode"
        android:visible="false"
        app:showAsAction="never">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/touch_mode"
                    android:checked="true"
                    android:title="@string/touch_move" />
                <item
                    android:id="@+id/click_mode"
                    android:title="@string/touch_screen_click" />
            </group>
        </menu>
    </item>
    <item
        android:id="@+id/group_open_key_is_adj_resolving"
        android:icon="@mipmap/system_keyboard"
        android:title="@string/open_key_is_adj_resolving"
        app:showAsAction="withText|never">
        <menu>
            <group android:checkableBehavior="single">
                <item
                    android:id="@+id/is_adjust"
                    android:checked="true"
                    android:title="@string/is" />
                <item
                    android:id="@+id/no_adjust"
                    android:title="@string/no" />
            </group>
        </menu>
    </item>
    <item
        android:id="@+id/disconnect"
        android:icon="@mipmap/disconnect"
        android:title="@string/disconnect"
        app:showAsAction="ifRoom" />
</menu>